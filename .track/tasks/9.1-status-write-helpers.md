---
id: "9.1"
title: "Add canonical task status write helpers"
status: done
mode: implement
priority: high
project_id: "9"
created: 2026-04-01
updated: 2026-04-01
depends_on: []
files:
  - "skills/runtime/scripts/track-common.sh"
  - "skills/work/scripts/track-task-status.sh"
  - "skills/work/scripts/track-complete.sh"
  - "skills/work/scripts/track-complete-writeback.sh"
  - "tests/test-complete.sh"
pr: "https://github.com/Hugopeck/track/pull/71"
---

## Context
Project 9 needs one shared bash-level path for writing canonical task status so PR lifecycle automation, completion, local wrappers, and reconciliation do not each invent their own frontmatter rewrite logic.

This task owns the foundation only: shared helpers for task-file writes and the completion-path refactor needed to reuse them. It should preserve bash 3.2 compatibility, existing writeback behavior, and current merged-task semantics while removing duplicate status-write logic where practical.

## Acceptance Criteria
- [ ] Shared helper(s) exist for writing canonical task status and updating `updated:` consistently
- [ ] The merged-task completion path reuses the shared status-write helper(s) instead of keeping a fully separate status rewrite implementation
- [ ] Helper behavior preserves existing completion semantics for `done`, `pr:`, and dependent-task unblocking
- [ ] `tests/test-complete.sh` covers the helper-backed completion path and passes

## Notes
Created by decompose from project 9.

2026-04-01: Added shared task-status helpers, refactored merged completion to reuse them, and verified with `bash tests/test-complete.sh` plus `bash tests/test-e2e-lifecycle.sh`.
