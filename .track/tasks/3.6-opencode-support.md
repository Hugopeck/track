---
id: "3.6"
title: "Document AGENTS.md-only setup for OpenCode"
status: done
mode: implement
priority: high
project_id: "3"
created: 2026-03-28
updated: 2026-03-30
depends_on: []
files:
  - "AGENTS.md"
  - "README.md"
pr: "https://github.com/Hugopeck/track/pull/35"
---

## Context
Initial scope assumed OpenCode might need `opencode.json`, but the final repo direction is simpler: shared repo-root `AGENTS.md` is enough. This task records the completed shift to an AGENTS-only setup path and README guidance.

## Steps
1. Reuse the repo `AGENTS.md` as the primary Track instruction source for OpenCode
2. Confirm no extra OpenCode-specific repo config is required in this repo
3. Document the shared-instruction setup in README
4. Keep the setup limited to launch-ready usage rather than broader agent abstractions
5. Verify the documented path matches the skill-only repo direction

## Acceptance Criteria
- [ ] `AGENTS.md` is the primary instruction source for OpenCode usage
- [ ] README includes a concrete shared-instruction setup path
- [ ] No OpenCode-specific repo config is required
- [ ] The task stays limited to launch-ready OpenCode support rather than broader agent abstractions
- [ ] The setup matches current documented OpenCode conventions

## Notes
Created after the 2026-03-28 project 3 scope reset.

2026-03-30 — Cleaned up the historical record to match the final implementation: shared `AGENTS.md` support only, no `opencode.json` artifact in this repo.
