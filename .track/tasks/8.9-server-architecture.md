---
id: "8.9"
title: "Write track-server architecture spec in .track/specs/"
status: todo
mode: plan
priority: high
project_id: "8"
created: 2026-03-30
updated: 2026-03-30
depends_on:
  - "8.1"
  - "1.2"
files:
  - ".track/specs/server-architecture.md"
pr: ""
---

## Context
Write the track-server architecture spec as a deliverable inside `.track/specs/`. This spec lives in this repo (not a separate one) so all contributors can reference it. The track-server repo will be built against this spec.

**Server stack:** Bun/TypeScript, `bun compile` for binary, `bun:sqlite` built-in.

**Key components:**
- HTTP server (event ingestion, REST API, dashboard serving)
- SQLite schema (tasks, events with nullable attribution, activity_summary)
- Matcher pipeline (deterministic first, LLM fallback for ambiguous)
- LLM integration (raw fetch against OpenAI-compatible API, no SDK)
- Task state sync (reads `.track/tasks/*.md` as source of truth)
- Dashboard (single-page HTML on same port)
- Install script (launchd/systemd registration)

**Architecture details in plan:** `.track/plans/track-oss-alignment.md` Part 2 has the full spec including directory layout, SQLite schema, processing pipeline, TypeScript interfaces, and install flow.

## Acceptance Criteria
- [ ] `.track/specs/server-architecture.md` exists
- [ ] Covers: directory layout, SQLite schema, API routes, matcher pipeline
- [ ] LLM integration spec uses OpenAI-compatible API (no vendor specifics)
- [ ] Install flow documented for macOS (launchd) and Linux (systemd)
- [ ] Dashboard wireframe included
- [ ] References EVENT-CONTRACT.md for payload schemas

## Notes
