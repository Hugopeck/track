---
id: "7.4"
title: "Script-level tests: validate error coverage and TODO accuracy"
status: review
mode: implement
priority: medium
project_id: "7"
created: 2026-03-27
updated: 2026-03-27
depends_on: []
files:
  - "tests/test-validate-extended.sh"
  - "tests/test-todo-extended.sh"
pr: ""
---

## Context
Two gaps in existing tests:

1. `track-validate.sh` has untested error paths: missing `.track/tasks` directory, malformed YAML frontmatter, missing required fields, invalid task ID format, PR context validation (branch references nonexistent task, terminal status task on branch, draft/ready mismatch).

2. `test-todo.sh` has only 4 assertions. Missing: task sorting by status/priority, dependency blocking logic, file overlap blocking, immediate starts filtering, cross-project dependency rendering, warnings section, empty project handling.

## Acceptance Criteria
- [ ] Every `print_error` call in `track-validate.sh` has at least one test triggering it
- [ ] Test: missing `.track/tasks` directory produces correct error
- [ ] Test: malformed YAML frontmatter triggers parse error
- [ ] Test: missing required field triggers specific error
- [ ] Test: invalid task ID format triggers format error
- [ ] Test: PR context validation (set env vars, verify branch/task checks)
- [ ] Test: TODO task sorting — active before todo, urgent before low
- [ ] Test: dependency blocking — task with undone dep excluded from immediate starts
- [ ] Test: file overlap blocking — todo task with files overlapping active task excluded
- [ ] All new tests verify both exit code AND stderr/stdout content

## Notes
Created from: project 7 — automated test coverage initiative.
