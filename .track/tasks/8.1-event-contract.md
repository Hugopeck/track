---
id: "8.1"
title: "Write event contract spec in .track/specs/"
status: todo
mode: implement
priority: high
project_id: "8"
created: 2026-03-30
updated: 2026-03-30
depends_on: []
files:
  - ".track/specs/event-contract.md"
pr: ""
---

## Context
The event contract is the key interface between Track components in this repo (protocol, skills, hooks, future local runtime). It defines event types, JSON payload schemas, the attribution model, and the LLM interface spec.

This must be written first because hook templates (8.3) and the server architecture spec (8.9) both implement against this contract.

Key design decisions already resolved:
- LLM-vendor-agnostic: OpenAI-compatible `/v1/chat/completions`
- Config: `base_url` + `api_key` + `model` (no vendor lock-in)
- Attribution is nullable: `null` = untracked activity, not lost
- Event types: track.commit, track.pr.opened, track.pr.ready, track.pr.merged, track.task.started, track.link

Reference: `.track/plans/track-oss-alignment.md` section 1B.

## Acceptance Criteria
- [ ] `.track/specs/event-contract.md` exists
- [ ] All 6 event types defined with JSON schemas
- [ ] Attribution model documented (nullable task_id, attribution source enum)
- [ ] LLM interface spec documented (OpenAI-compatible, no vendor specifics)
- [ ] Payload version field included for forward compatibility

## Notes
