---
title: Rename `init` to `setup-track`
created: 2026-04-01
task_id: "8.11"
project_id: "8"
---

# Rename `init` to `setup-track`

## Summary
- Workspace branch is already renamed to `Hugopeck/setup-track`.
- Goal: eliminate the collision with Claude’s built-in `init` by renaming the Track setup skill across the public surface, command, and repo path.
- Cutover: rename `skills/init/` to `skills/setup-track/`, change `name: init` to `name: setup-track`, and replace `/track:init` with `/track:setup-track`.
- Compatibility: no runtime alias; keep exactly one migration note in `README.md:1`; leave completed historical `.track/` records untouched.

## Public Changes
- Skill directory moves from `skills/init/SKILL.md:1` to `skills/setup-track/SKILL.md:1`.
- Skill frontmatter changes from `name: init` to `name: setup-track`.
- User command changes from `/track:init` to `/track:setup-track`.
- Installed skill symlink name changes automatically via `install.sh:1` because it uses the folder basename.
- Manifest source paths move from `skills/init/assets/...` to `skills/setup-track/assets/...`.
- Hook marker text changes from `Deployed by track init` to `Deployed by track setup-track`.
- Treat the change as a breaking rename; use a breaking conventional commit such as `feat(skills)!: rename init to setup-track`.

## Implementation Steps
1. **Track Bookkeeping**
- Create a new project-8 task, e.g. `.track/tasks/8.11-rename-init-to-setup-track.md:1`, with `mode: implement` and file scopes covering docs, tests, and `skills/setup-track/**`.
- Save this plan to `.track/plans/8.11-setup-track-rename.md:1`.
- After task-file changes, run `bash .track/scripts/track-validate.sh` and `bash .track/scripts/track-todo.sh --local --offline`.

2. **Move Renamed Surfaces**
- Run `git mv skills/init skills/setup-track`.
- Run `git mv tests/test-init-skill.sh tests/test-setup-track-skill.sh`.
- Do not rename unrelated directories such as `skills/todo/` or `skills/update-track/`.

3. **Update the Setup Skill Contract**
- Edit `skills/setup-track/SKILL.md:1` to set `name: setup-track`.
- Replace every supported `/track:init` reference with `/track:setup-track`.
- Replace internal repo-path references from `skills/init/...` to `skills/setup-track/...`.
- Preserve the required protocol sections exactly: “What This Skill Owns”, “Operating Modes”, “Definition of Done”, “Closing Message Matrix”, and “Do Not”.

4. **Update Bundled Assets and Path-Sensitive Internals**
- Edit `skills/setup-track/assets/install-manifest.json:1` so every `source` path points at `skills/setup-track/assets/...`.
- Update hook header comments in `skills/setup-track/assets/hooks/commit-msg:1` and `skills/setup-track/assets/hooks/post-commit:1` from `track init` to `track setup-track`.
- Keep workflow filenames unchanged in `skills/setup-track/assets/workflows/*.yml`; only the repo paths that point to them change.

5. **Update Supported-Surface References**
- Update skill/docs/runtime references in:
  - `skills/README.md:1`
  - `README.md:1`
  - `TRACK.md:1`
  - `docs/skills-guide.md:1`
  - `skills/create/SKILL.md:1`
  - `skills/decompose/SKILL.md:1`
  - `skills/work/SKILL.md:1`
  - `skills/todo/SKILL.md:1`
  - `skills/todo/scripts/track-todo.sh:827`
  - `skills/validate/scripts/track-validate.sh:470`
- Update marketing/demo content in:
  - `growth/articles/devto-launch.md:123`
  - `growth/videos/demo-5min-script.md:32`
  - `growth/videos/60sec-script.md:17`
  - `growth/posts/reddit-claude-ai.md:20`
  - `growth/posts/discord-intro.md:29`
  - `growth/posts/x-launch-thread.md:57`
- Replace the public skill label `init` with `setup-track` only where it denotes the current supported skill; do not rewrite generic English uses of “init” that are not skill names.

6. **Update `AGENTS.md` the Repo-Approved Way**
- Manually edit the non-generated top section in `AGENTS.md:1` for:
  - the repo-layout bullet `skills/setup-track/assets/`
  - the skill-inventory row `setup-track`
  - the common-scope list if commit scopes should match the renamed subsystem
- Edit `TRACK.md:1` first for canonical Track guidance.
- Regenerate the embedded Track block in `AGENTS.md:1` with `bash tools/render-track-section.sh`.
- Do not hand-edit the `<!-- TRACK:START --> ... <!-- TRACK:END -->` block after regeneration.

7. **Update Live Track Planning Records, Not Historical Archives**
- Update current, non-done command examples in:
  - `.track/tasks/2.1-demo-gif.md:20`
  - `.track/tasks/2.2-demo-video.md:25`
  - `.track/tasks/4.7-product-hunt.md:31`
  - `.track/projects/8-track-oss-alignment.md:35`
- Leave completed historical records unchanged:
  - done task files in `.track/tasks/`
  - `.track/plans/`
  - `.track/specs/`
- Also leave the stale `scaffold` references in `.track/tasks/7.5-script-mirror-test.md:17` and `.track/projects/7-test-coverage.md:33` for a separate cleanup task; they describe pre-existing architecture drift, not this rename.

## Tests and Validation
- Update and run focused regression tests:
  - `bash tests/test-setup-track-skill.sh`
  - `bash tests/test-install-script.sh`
  - `bash tests/test-commit-lint.sh`
  - `bash tests/test-conventional-commit-lint.sh`
  - `bash tests/test-event-contract.sh`
  - `bash tests/test-ruleset.sh`
  - `bash tests/test-complete.sh`
  - `bash tests/test-complete-writeback.sh`
  - `bash tests/test-work-skill.sh`
- Run full repo verification:
  - `bash .track/scripts/track-validate.sh` if any `.track/` files changed
  - `bash .track/scripts/track-todo.sh --local --offline` after `.track/` edits
  - `bash tests/run-all.sh`
- Grep-based exit criteria:
  - no supported-surface matches for `name: init`, `skills/init/`, or `/track:init`
  - allowed leftovers are only historical archives and the explicitly deferred stale `scaffold` backlog references above

## Acceptance Criteria
- `skills/setup-track/SKILL.md:1` exists and declares `name: setup-track`.
- `skills/init/` no longer exists.
- User docs and skill bodies consistently instruct `/track:setup-track`.
- `install.sh:1` installs a `setup-track` skill because the folder basename changed.
- All tests pass after path and string updates.
- `AGENTS.md:1` matches `TRACK.md:1` in its Track-managed section after regeneration.
- The only old-name mentions left in-repo are intentionally preserved historical or deferred backlog records.

## Assumptions and Defaults
- `/track:setup-track` is the new canonical command; `/track:init` gets no alias.
- The migration note lives in `README.md:1` only; `TRACK.md:1` and skill bodies stay clean and forward-only.
- This is a breaking user-facing rename, so the implementation should use a breaking conventional commit.
- No repo-tracked files are changed during planning; the actual implementation should follow this plan in a separate execution turn.
