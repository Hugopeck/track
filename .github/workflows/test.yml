name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-scripts:
    name: Test Track Scripts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Track test suite
        run: bash tests/run-all.sh
